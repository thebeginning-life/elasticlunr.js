language: node_js
addons:
  chrome: stable
node_js:
  - "9"
install:
  - npm install
script:
  - npm run build
  - npm run test
  - 'if [ "$TRAVIS_PULL_REQUEST" != "false" ]; then npm run prepare:integration && npm run test:integration; fi'